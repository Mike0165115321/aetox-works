# Aetox Works — Tools
# เครื่องมือที่ agent ใช้

from typing import Any


# ที่ว่างสำหรับ tools ต่างๆ
# เช่น: web_search, file_manager, video_renderer, deploy_website ฯลฯ

TOOL_REGISTRY = {
    "web_search": "ค้นหาข้อมูลจากเว็บ",
    "file_manager": "จัดการไฟล์",
    "video_render": "render วิดีโอด้วย HyperFrames",
    "deploy": "deploy ขึ้น server",
}
